# gpu-patch.yaml
machine:
  install:
    extraKernelArgs:
      - i915.force_probe=e20b
      - i915.enable_guc=2
  kernel:
    modules:
      - name: i915
      - name: drm
      - name: drm_kms_helper